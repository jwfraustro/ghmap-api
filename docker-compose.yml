version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=True
    command: uvicorn ghmap_api.main:app --host 0.0.0.0 --port 8000 --reload
